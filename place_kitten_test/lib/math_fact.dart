import 'package:flutter/material.dart';

class MathFact extends StatelessWidget {
  final String text;
  final int num;
  const MathFact({this.text = "", this.num = 0, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
